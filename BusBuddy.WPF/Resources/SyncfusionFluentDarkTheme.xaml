<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
    xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
    xmlns:editors="clr-namespace:Syncfusion.Windows.Controls.Input;assembly=Syncfusion.SfInput.Wpf"
    xmlns:buttons="clr-namespace:Syncfusion.Windows.Tools.Controls;assembly=Syncfusion.Tools.Wpf"
    xmlns:progressbar="clr-namespace:Syncfusion.UI.Xaml.ProgressBar;assembly=Syncfusion.SfProgressBar.WPF"
    xmlns:chart="clr-namespace:Syncfusion.UI.Xaml.Charts;assembly=Syncfusion.SfChart.WPF"
    xmlns:gauge="clr-namespace:Syncfusion.UI.Xaml.Gauges;assembly=Syncfusion.SfGauge.WPF">

    <!-- 🎨 SYNCFUSION FLUENT DARK THEME BASELINE FRAMEWORK 🎨 -->
    <!-- This resource dictionary establishes the unified theme baseline for all BusBuddy views -->
    <!-- Based on successful MainWindow and EnhancedDashboardView implementations -->

    <!-- ╔══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╗ -->
    <!-- ║                                                                                                                                                                          ║ -->
    <!-- ║                                                      🔧 SYNCFUSION CONTROL UNIVERSAL STANDARDS REFERENCE 🔧                                                         ║ -->
    <!-- ║                                                                                                                                                                          ║ -->
    <!-- ║  This section provides a comprehensive reference for all Syncfusion controls and their proper implementation patterns.                                               ║ -->
    <!-- ║  Use this as the authoritative guide to avoid documentation lookups and ensure consistency across the application.                                                    ║ -->
    <!-- ║                                                                                                                                                                          ║ -->
    <!-- ╚══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╝ -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 📋 NAMESPACE DECLARATIONS STANDARD -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        REQUIRED NAMESPACE DECLARATIONS for ALL Syncfusion controls:

        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"

        ⚠️ CRITICAL: Always use "syncfusion:" prefix — NEVER use "sf:" or other prefixes
        ⚠️ CRITICAL: Apply FluentDark theme using: syncfusionskin:SfSkinManager.VisualStyle="FluentDark"
    -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 🔲 BUTTON CONTROLS STANDARD -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        SYNCFUSION BUTTONADV CONTROL:

        ✅ CORRECT USAGE:
        <syncfusion:ButtonAdv Label="Button Text"
                              Command="{Binding SomeCommand}"
                              Style="{StaticResource BusBuddyPrimaryButtonStyle}"
                              syncfusionskin:SfSkinManager.VisualStyle="FluentDark"/>

        ❌ INCORRECT USAGE:
        <syncfusion:ButtonAdv Content="Button Text"/>  ← WRONG: Use Label, not Content
        <Button Content="Text"/>                       ← WRONG: Use ButtonAdv, not Button
        <sf:ButtonAdv Label="Text"/>                   ← WRONG: Use syncfusion prefix, not sf

        AVAILABLE STYLES:
        — BusBuddyPrimaryButtonStyle      → Main action buttons (Add, Save, etc.)
        — BusBuddySecondaryButtonStyle    → Secondary actions (Edit, Cancel, etc.)
        — BusBuddyNavigationButtonStyle   → Navigation/menu buttons
        — BusBuddyIconButtonStyle         → Icon-only buttons (toolbar, pagination)

        ICON BUTTONS PATTERN:
        <syncfusion:ButtonAdv Label="&#xE76B;"
                              FontFamily="Segoe MDL2 Assets"
                              Style="{StaticResource BusBuddyIconButtonStyle}"/>
    -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 📝 INPUT CONTROLS STANDARD -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        SYNCFUSION COMBOBOXADV CONTROL:

        ✅ CORRECT USAGE:
        <syncfusion:ComboBoxAdv SelectedValue="{Binding SomeProperty}"
                                Style="{StaticResource BusBuddyComboBoxAdvStyle}"
                                syncfusionskin:SfSkinManager.VisualStyle="FluentDark">
            <syncfusion:ComboBoxItemAdv Content="Option 1"/>
            <syncfusion:ComboBoxItemAdv Content="Option 2"/>
        </syncfusion:ComboBoxAdv>

        ❌ INCORRECT USAGE:
        <ComboBox>                          ← WRONG: Use ComboBoxAdv, not ComboBox
            <ComboBoxItem Content="Text"/>  ← WRONG: Use ComboBoxItemAdv
        </ComboBox>

        SYNCFUSION TEXTBOXEXT CONTROL:

        ✅ CORRECT USAGE:
        <syncfusion:TextBoxExt Text="{Binding SomeText}"
                               syncfusionskin:SfSkinManager.VisualStyle="FluentDark"
                               Background="{StaticResource FluentDarkSurfaceBrush}"
                               Foreground="{StaticResource FluentDarkPrimaryTextBrush}"/>
    -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 📊 DATA GRID CONTROLS STANDARD -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        SYNCFUSION SFDATAGRID CONTROL:

        ✅ CORRECT USAGE:
        <syncfusion:SfDataGrid ItemsSource="{Binding DataCollection}"
                               syncfusionskin:SfSkinManager.VisualStyle="FluentDark"
                               AutoGenerateColumns="False"
                               AllowEditing="True"
                               AllowSorting="True"
                               AllowFiltering="True">
            <syncfusion:SfDataGrid.Columns>
                <syncfusion:GridTextColumn MappingName="PropertyName" HeaderText="Column Header"/>
                <syncfusion:GridDateTimeColumn MappingName="DateProperty" HeaderText="Date"/>
                <syncfusion:GridNumericColumn MappingName="NumberProperty" HeaderText="Number"/>
            </syncfusion:SfDataGrid.Columns>
        </syncfusion:SfDataGrid>

        AVAILABLE COLUMN TYPES:
        — GridTextColumn           → Text data
        — GridNumericColumn        → Numeric data
        — GridDateTimeColumn       → Date/time data
        — GridComboBoxColumn       → Dropdown selection
        — GridCheckBoxColumn       → Boolean/checkbox
        — GridTemplateColumn       → Custom templates
    -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 🏗️ LAYOUT CONTROLS STANDARD -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        SYNCFUSION DOCKINGMANAGER CONTROL:

        CORRECT USAGE:
        <syncfusion:DockingManager UseDocumentContainer="True"
                                   PersistState="False"
                                   ContainerMode="TDI"
                                   Style="{StaticResource BusBuddyDockingManagerStyle}"
                                   syncfusionskin:SfSkinManager.VisualStyle="FluentDark">
        </syncfusion:DockingManager>

        INCORRECT PROPERTIES: DockingManager does not have BorderBrush property
        BorderThickness="1" WRONG: DockingManager does not have BorderThickness property

        VALID DOCKINGMANAGER PROPERTIES:
        — UseDocumentContainer Boolean
        — PersistState Boolean
        — ContainerMode TDI, MDI, etc.
        — Background            → Brush (standard WPF property)
        — syncfusionskin:SfSkinManager.VisualStyle → "FluentDark"
    -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 📈 PROGRESS CONTROLS STANDARD -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        SYNCFUSION PROGRESS CONTROLS:

        ✅ CIRCULAR PROGRESS BAR:
        <syncfusion:SfCircularProgressBar Value="{Binding ProgressValue}"
                                          Style="{StaticResource BusBuddySfCircularProgressBarStyle}"
                                          syncfusionskin:SfSkinManager.VisualStyle="FluentDark"/>

        ✅ LINEAR PROGRESS BAR:
        <syncfusion:SfLinearProgressBar Value="{Binding ProgressValue}"
                                        Style="{StaticResource BusBuddySfLinearProgressBarStyle}"
                                        syncfusionskin:SfSkinManager.VisualStyle="FluentDark"/>

        ✅ BUSY INDICATOR:
        <syncfusion:SfBusyIndicator IsBusy="{Binding IsBusy}"
                                    syncfusionskin:SfSkinManager.VisualStyle="FluentDark"/>
    -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 📊 GAUGE CONTROLS STANDARD -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        SYNCFUSION GAUGE CONTROLS:

        ✅ CIRCULAR GAUGE:
        <syncfusion:SfCircularGauge Style="{StaticResource BusBuddySfCircularGaugeStyle}"
                                    syncfusionskin:SfSkinManager.VisualStyle="FluentDark">
            <syncfusion:SfCircularGauge.Scales>
                <syncfusion:CircularScale StartValue="0" EndValue="100"/>
            </syncfusion:SfCircularGauge.Scales>
        </syncfusion:SfCircularGauge>
    -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 🎨 THEMING IMPLEMENTATION PATTERN -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        UNIVERSAL THEMING PATTERN FOR ALL SYNCFUSION CONTROLS:

        1. ALWAYS add namespace declaration: syncfusionskin:SfSkinManager.VisualStyle="FluentDark"
        2. ALWAYS apply baseline brushes from this resource dictionary
        3. ALWAYS use the predefined styles from this file
        4. NEVER mix standard WPF controls with Syncfusion controls in the same context

        EXAMPLE COMPLETE CONTROL IMPLEMENTATION:
        <syncfusion:ButtonAdv Label="Complete Example"
                              Command="{Binding ExampleCommand}"
                              Style="{StaticResource BusBuddyPrimaryButtonStyle}"
                              syncfusionskin:SfSkinManager.VisualStyle="FluentDark"
                              Background="{StaticResource FluentDarkAccentBrush}"
                              Foreground="{StaticResource FluentDarkPrimaryTextBrush}"/>
    -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 🔄 CONTROL MIGRATION CHECKLIST -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        WHEN CONVERTING STANDARD WPF TO SYNCFUSION CONTROLS:

        ✅ Button → syncfusion:ButtonAdv
           — Change Content="..." to Label="..."
           — Add syncfusionskin:SfSkinManager.VisualStyle="FluentDark"
           — Apply appropriate BusBuddy style

        ✅ ComboBox → syncfusion:ComboBoxAdv
           — Change ComboBoxItem to syncfusion:ComboBoxItemAdv
           — Add syncfusionskin:SfSkinManager.VisualStyle="FluentDark"
           — Apply BusBuddyComboBoxAdvStyle

        ✅ TextBox → syncfusion:TextBoxExt
           — Add syncfusionskin:SfSkinManager.VisualStyle="FluentDark"
           — Apply baseline brushes for colors

        ✅ DataGrid → syncfusion:SfDataGrid
           — Convert columns to appropriate GridXXXColumn types
           — Add syncfusionskin:SfSkinManager.VisualStyle="FluentDark"

        ✅ ProgressBar → syncfusion:SfLinearProgressBar or SfCircularProgressBar
           — Add syncfusionskin:SfSkinManager.VisualStyle="FluentDark"
           — Apply appropriate BusBuddy style
    -->

    <!-- ==================== THEME COLOR PALETTE ==================== -->

    <!-- Primary Theme Colors — FluentDark Palette -->
    <Color x:Key="FluentDarkPrimaryColor">#2D2D30</Color>
    <Color x:Key="FluentDarkSecondaryColor">#3F3F46</Color>
    <Color x:Key="FluentDarkAccentColor">#007ACC</Color>
    <Color x:Key="FluentDarkSuccessColor">#2ECC71</Color>
    <Color x:Key="FluentDarkWarningColor">#F39C12</Color>
    <Color x:Key="FluentDarkErrorColor">#E74C3C</Color>

    <!-- Text Colors -->
    <Color x:Key="FluentDarkPrimaryTextColor">#F1F1F1</Color>
    <Color x:Key="FluentDarkSecondaryTextColor">#CCCCCC</Color>
    <Color x:Key="FluentDarkMutedTextColor">#999999</Color>

    <!-- Background Colors -->
    <Color x:Key="FluentDarkBackgroundColor">#2D2D30</Color>
    <Color x:Key="FluentDarkSurfaceColor">#3F3F46</Color>
    <Color x:Key="FluentDarkHoverColor">#4A4A51</Color>

    <!-- Border Colors -->
    <Color x:Key="FluentDarkBorderColor">#3F3F46</Color>
    <Color x:Key="FluentDarkFocusBorderColor">#007ACC</Color>

    <!-- ==================== SOLID COLOR BRUSHES ==================== -->

    <SolidColorBrush x:Key="FluentDarkPrimaryBrush" Color="{StaticResource FluentDarkPrimaryColor}"/>
    <SolidColorBrush x:Key="FluentDarkSecondaryBrush" Color="{StaticResource FluentDarkSecondaryColor}"/>
    <SolidColorBrush x:Key="FluentDarkAccentBrush" Color="{StaticResource FluentDarkAccentColor}"/>
    <SolidColorBrush x:Key="FluentDarkSuccessBrush" Color="{StaticResource FluentDarkSuccessColor}"/>
    <SolidColorBrush x:Key="FluentDarkWarningBrush" Color="{StaticResource FluentDarkWarningColor}"/>
    <SolidColorBrush x:Key="FluentDarkErrorBrush" Color="{StaticResource FluentDarkErrorColor}"/>

    <SolidColorBrush x:Key="FluentDarkPrimaryTextBrush" Color="{StaticResource FluentDarkPrimaryTextColor}"/>
    <SolidColorBrush x:Key="FluentDarkSecondaryTextBrush" Color="{StaticResource FluentDarkSecondaryTextColor}"/>
    <SolidColorBrush x:Key="FluentDarkMutedTextBrush" Color="{StaticResource FluentDarkMutedTextColor}"/>

    <SolidColorBrush x:Key="FluentDarkBackgroundBrush" Color="{StaticResource FluentDarkBackgroundColor}"/>
    <SolidColorBrush x:Key="FluentDarkSurfaceBrush" Color="{StaticResource FluentDarkSurfaceColor}"/>
    <SolidColorBrush x:Key="FluentDarkHoverBrush" Color="{StaticResource FluentDarkHoverColor}"/>

    <!-- Additional brushes for UI requirements -->
    <SolidColorBrush x:Key="FluentDarkForegroundBrush" Color="{StaticResource FluentDarkPrimaryTextColor}"/>
    <SolidColorBrush x:Key="FluentDarkOverlayBrush" Color="#494952"/>
    <SolidColorBrush x:Key="FluentDarkPurpleBrush" Color="#9B59B6"/>

    <SolidColorBrush x:Key="FluentDarkBorderBrush" Color="{StaticResource FluentDarkBorderColor}"/>
    <SolidColorBrush x:Key="FluentDarkFocusBorderBrush" Color="{StaticResource FluentDarkFocusBorderColor}"/>

    <!-- ==================== SYNCFUSION CONTROL BASE STYLES ==================== -->

    <!-- Base UserControl Style — Apply to all child forms -->
    <Style x:Key="BusBuddyUserControlStyle" TargetType="UserControl">
        <Setter Property="Background" Value="{StaticResource FluentDarkBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
    </Style>

    <!-- Base Window Style -->
    <Style x:Key="BusBuddyWindowStyle" TargetType="Window">
        <Setter Property="Background" Value="{StaticResource FluentDarkBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="WindowStyle" Value="SingleBorderWindow"/>
        <Setter Property="ResizeMode" Value="CanResize"/>
    </Style>

    <!-- ==================== SYNCFUSION BUTTONADV STYLES ==================== -->

    <!-- Primary Button Style -->
    <Style x:Key="BusBuddyPrimaryButtonStyle" TargetType="syncfusion:ButtonAdv">
        <Setter Property="Background" Value="{StaticResource FluentDarkAccentBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FluentDarkAccentBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="SizeMode" Value="Normal"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#0066CC"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#005AA3"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Secondary Button Style -->
    <Style x:Key="BusBuddySecondaryButtonStyle" TargetType="syncfusion:ButtonAdv">
        <Setter Property="Background" Value="{StaticResource FluentDarkSecondaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FluentDarkBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="SizeMode" Value="Normal"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource FluentDarkHoverBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Navigation Button Style (for header/navigation) -->
    <Style x:Key="BusBuddyNavigationButtonStyle" TargetType="syncfusion:ButtonAdv">
        <Setter Property="Background" Value="#003366"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="3"/>
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="SizeMode" Value="Normal"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#004080"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Icon Button Style (for toolbar buttons) -->
    <Style x:Key="BusBuddyIconButtonStyle" TargetType="syncfusion:ButtonAdv">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="SizeMode" Value="Small"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource FluentDarkHoverBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ==================== ADDITIONAL SYNCFUSION CONTROL STYLES ==================== -->

    <!-- SfDataGrid Style -->
    <Style x:Key="BusBuddySfDataGridStyle" TargetType="syncfusion:SfDataGrid">
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="Background" Value="{StaticResource FluentDarkBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FluentDarkBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="AllowEditing" Value="True"/>
        <Setter Property="AllowSorting" Value="True"/>
        <Setter Property="AllowFiltering" Value="True"/>
        <Setter Property="RowHeight" Value="40"/>
        <Setter Property="HeaderRowHeight" Value="45"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
    </Style>

    <!-- SfBusyIndicator Style -->
    <Style x:Key="BusBuddySfBusyIndicatorStyle" TargetType="syncfusion:SfBusyIndicator">
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkAccentBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="AnimationType" Value="DoubleCircle"/>
    </Style>

    <!-- DatePicker Style -->
    <Style x:Key="BusBuddyDatePickerStyle" TargetType="syncfusion:SfDatePicker">
        <Setter Property="Background" Value="{StaticResource FluentDarkSurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FluentDarkBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
    </Style>

    <!-- ==================== SPECIALIZED CONTROL STYLES ==================== -->

    <!-- Delete Button Style (Special Styling for Destructive Actions) -->
    <Style x:Key="BusBuddyDeleteButtonStyle" TargetType="syncfusion:ButtonAdv">
        <Setter Property="Background" Value="{StaticResource FluentDarkErrorBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FluentDarkErrorBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="SizeMode" Value="Normal"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#CC1F1F"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#A11919"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Success Button Style (For Save, Confirm Actions) -->
    <Style x:Key="BusBuddySuccessButtonStyle" TargetType="syncfusion:ButtonAdv">
        <Setter Property="Background" Value="{StaticResource FluentDarkSuccessBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FluentDarkSuccessBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="SizeMode" Value="Normal"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#28B463"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#239B56"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Warning Button Style (For Caution Actions) -->
    <Style x:Key="BusBuddyWarningButtonStyle" TargetType="syncfusion:ButtonAdv">
        <Setter Property="Background" Value="{StaticResource FluentDarkWarningBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FluentDarkWarningBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="SizeMode" Value="Normal"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#E67E22"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#D35400"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ==================== SYNCFUSION LAYOUT CONTROL STYLES ==================== -->

    <!-- DockingManager Style -->
    <Style x:Key="BusBuddyDockingManagerStyle" TargetType="syncfusion:DockingManager">
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="UseDocumentContainer" Value="True"/>
        <Setter Property="PersistState" Value="False"/>
        <Setter Property="ContainerMode" Value="TDI"/>
        <Setter Property="Background" Value="{StaticResource FluentDarkBackgroundBrush}"/>
    </Style>

    <!-- ==================== SYNCFUSION PROGRESS CONTROL STYLES ==================== -->

    <!-- SfCircularProgressBar Style -->
    <Style x:Key="BusBuddySfCircularProgressBarStyle" TargetType="syncfusion:SfCircularProgressBar">
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
    </Style>

    <!-- SfLinearProgressBar Style -->
    <Style x:Key="BusBuddySfLinearProgressBarStyle" TargetType="syncfusion:SfLinearProgressBar">
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
    </Style>

    <!-- ==================== SYNCFUSION GAUGE CONTROL STYLES ==================== -->

    <!-- SfCircularGauge Style -->
    <Style x:Key="BusBuddySfCircularGaugeStyle" TargetType="syncfusion:SfCircularGauge">
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <!-- ==================== DATA DISPLAY STYLES ==================== -->

    <!-- Tile Header Style -->
    <Style x:Key="BusBuddyTileHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="Margin" Value="0,0,0,12"/>
    </Style>

    <!-- Tile Content Style -->
    <Style x:Key="BusBuddyTileContentStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkSecondaryTextBrush}"/>
        <Setter Property="Margin" Value="0,0,0,6"/>
    </Style>

    <!-- Tile Value Style -->
    <Style x:Key="BusBuddyTileValueStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkAccentBrush}"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <!-- Status Indicator Style -->
    <Style x:Key="BusBuddyStatusIndicatorStyle" TargetType="Ellipse">
        <Setter Property="Width" Value="12"/>
        <Setter Property="Height" Value="12"/>
        <Setter Property="Margin" Value="8,0,0,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ==================== DASHBOARD TILE STYLES ==================== -->

    <!-- Base Tile Style -->
    <Style x:Key="BusBuddyDashboardTileStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource FluentDarkSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FluentDarkBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="8" Direction="270" ShadowDepth="2" Opacity="0.3"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource FluentDarkHoverBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource FluentDarkAccentBrush}"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="12" Direction="270" ShadowDepth="3" Opacity="0.4"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Performance Tile Style -->
    <Style x:Key="BusBuddyPerformanceTileStyle" TargetType="Border" BasedOn="{StaticResource BusBuddyDashboardTileStyle}">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#007ACC" Offset="0"/>
                    <GradientStop Color="#005AA3" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush" Value="{StaticResource FluentDarkAccentBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
    </Style>

    <!-- ==================== NAVIGATION STYLES ==================== -->

    <!-- Navigation Panel Style -->
    <Style x:Key="BusBuddyNavigationPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource FluentDarkPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FluentDarkBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,0"/>
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
    </Style>

    <!-- Navigation ListView Style -->
    <Style x:Key="BusBuddyNavigationListViewStyle" TargetType="ListView">
        <Setter Property="Background" Value="{StaticResource FluentDarkPrimaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="syncfusionskin:SfSkinManager.VisualStyle" Value="FluentDark"/>
    </Style>

    <!-- Navigation ListView Item Style -->
    <Style x:Key="BusBuddyNavigationListViewItemStyle" TargetType="ListViewItem">
        <Setter Property="Padding" Value="15,12"/>
        <Setter Property="Background" Value="{StaticResource FluentDarkPrimaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource FluentDarkHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource FluentDarkAccentBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource FluentDarkPrimaryTextBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ==================== FORM CONTENT STYLES ==================== -->

    <!-- Form Header Style -->
    <Style x:Key="BusBuddyFormHeaderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource FluentDarkAccentBrush}"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
    </Style>

    <!-- Form Content Style -->
    <Style x:Key="BusBuddyFormContentStyle" TargetType="Grid">
        <Setter Property="Background" Value="{StaticResource FluentDarkBackgroundBrush}"/>
        <Setter Property="Margin" Value="16"/>
    </Style>

    <!-- ╔══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╗ -->
    <!-- ║                                                                                                                                                                          ║ -->
    <!-- ║                                                     📚 QUICK IMPLEMENTATION GUIDE 📚                                                                                 ║ -->
    <!-- ║                                                                                                                                                                          ║ -->
    <!-- ║  Copy and paste these patterns directly into your XAML files for instant Syncfusion FluentDark theme compliance.                                                     ║ -->
    <!-- ║                                                                                                                                                                          ║ -->
    <!-- ╚══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╝ -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 🏗️ USERCONTROL SETUP TEMPLATE -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        COPY THIS USERCONTROL DECLARATION FOR ALL CHILD FORMS:

        <UserControl x:Class="YourNamespace.YourView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
                     xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
                     Style="{StaticResource BusBuddyUserControlStyle}">

            [Your content here]
        </UserControl>
    -->

    <!-- 🔲 BUTTON TEMPLATES -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        COPY THESE BUTTON PATTERNS:

        PRIMARY ACTION (Add, Save, Submit):
        <syncfusion:ButtonAdv Label="💾 Save Changes"
                              Command="{Binding SaveCommand}"
                              Style="{StaticResource BusBuddyPrimaryButtonStyle}"
                              Margin="5"/>

        SECONDARY ACTION (Edit, Cancel):
        <syncfusion:ButtonAdv Label="✏️ Edit"
                              Command="{Binding EditCommand}"
                              Style="{StaticResource BusBuddySecondaryButtonStyle}"
                              Margin="5"/>

        DELETE ACTION:
        <syncfusion:ButtonAdv Label="🗑️ Delete"
                              Command="{Binding DeleteCommand}"
                              Style="{StaticResource BusBuddyDeleteButtonStyle}"
                              Margin="5"/>

        SUCCESS ACTION (Confirm, Approve):
        <syncfusion:ButtonAdv Label="✅ Confirm"
                              Command="{Binding ConfirmCommand}"
                              Style="{StaticResource BusBuddySuccessButtonStyle}"
                              Margin="5"/>

        ICON BUTTON (Toolbar, Pagination):
        <syncfusion:ButtonAdv Label="&#xE76B;"
                              FontFamily="Segoe MDL2 Assets"
                              Command="{Binding NavigateCommand}"
                              Style="{StaticResource BusBuddyIconButtonStyle}"
                              ToolTip="Previous Page"/>
    -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 📊 DATA GRID TEMPLATE -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        COPY THIS DATA GRID PATTERN:

        <syncfusion:SfDataGrid ItemsSource="{Binding YourDataCollection}"
                               Style="{StaticResource BusBuddySfDataGridStyle}"
                               SelectedItem="{Binding SelectedItem}"
                               syncfusionskin:SfSkinManager.VisualStyle="FluentDark">
            <syncfusion:SfDataGrid.Columns>
                <syncfusion:GridTextColumn MappingName="PropertyName" HeaderText="Column Header" Width="150"/>
                <syncfusion:GridDateTimeColumn MappingName="DateProperty" HeaderText="Date" Width="120"/>
                <syncfusion:GridNumericColumn MappingName="NumberProperty" HeaderText="Amount" Width="100"/>
                <syncfusion:GridComboBoxColumn MappingName="StatusProperty" HeaderText="Status" Width="120"/>
            </syncfusion:SfDataGrid.Columns>
        </syncfusion:SfDataGrid>
    -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 📝 INPUT CONTROLS TEMPLATE -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        COPY THESE INPUT CONTROL PATTERNS:

        TEXT INPUT:
        <syncfusion:TextBoxExt Text="{Binding YourProperty}"
                               Style="{StaticResource BusBuddyTextBoxExtStyle}"
                               syncfusionskin:SfSkinManager.VisualStyle="FluentDark"/>

        DROPDOWN SELECTION:
        <syncfusion:ComboBoxAdv ItemsSource="{Binding OptionsCollection}"
                                SelectedValue="{Binding SelectedOption}"
                                Style="{StaticResource BusBuddyComboBoxAdvStyle}"
                                syncfusionskin:SfSkinManager.VisualStyle="FluentDark">
            <syncfusion:ComboBoxItemAdv Content="Option 1"/>
            <syncfusion:ComboBoxItemAdv Content="Option 2"/>
        </syncfusion:ComboBoxAdv>

        DATE SELECTION:
        <syncfusion:SfDatePicker Value="{Binding SelectedDate}"
                                 Style="{StaticResource BusBuddyDatePickerStyle}"
                                 syncfusionskin:SfSkinManager.VisualStyle="FluentDark"/>

        NUMERIC INPUT:
        <syncfusion:SfNumericUpDown Value="{Binding NumericValue}"
                                    Style="{StaticResource BusBuddyNumericUpDownStyle}"
                                    syncfusionskin:SfSkinManager.VisualStyle="FluentDark"/>
    -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- ⏳ LOADING INDICATORS TEMPLATE -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        COPY THESE LOADING INDICATOR PATTERNS:

        BUSY INDICATOR (Loading Overlay):
        <syncfusion:SfBusyIndicator IsBusy="{Binding IsLoading}"
                                    Style="{StaticResource BusBuddySfBusyIndicatorStyle}"
                                    BusyContent="Loading..."
                                    syncfusionskin:SfSkinManager.VisualStyle="FluentDark"/>

        PROGRESS BAR (Linear):
        <syncfusion:SfLinearProgressBar Value="{Binding ProgressValue}"
                                        Style="{StaticResource BusBuddySfLinearProgressBarStyle}"
                                        syncfusionskin:SfSkinManager.VisualStyle="FluentDark"/>

        PROGRESS BAR (Circular):
        <syncfusion:SfCircularProgressBar Value="{Binding ProgressValue}"
                                          Style="{StaticResource BusBuddySfCircularProgressBarStyle}"
                                          syncfusionskin:SfSkinManager.VisualStyle="FluentDark"/>
    -->

    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!-- 🎨 TEXT STYLING TEMPLATE -->
    <!-- ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════ -->
    <!--
        COPY THESE TEXT STYLING PATTERNS:

        FORM HEADERS:
        <TextBlock Text="Section Title" Style="{StaticResource BusBuddyTileHeaderStyle}"/>

        CONTENT TEXT:
        <TextBlock Text="Description text" Style="{StaticResource BusBuddyTileContentStyle}"/>

        IMPORTANT VALUES:
        <TextBlock Text="{Binding ImportantValue}" Style="{StaticResource BusBuddyTileValueStyle}"/>

        COLORED TEXT:
        <TextBlock Text="Information" Foreground="{StaticResource FluentDarkPrimaryTextBrush}"/>
        <TextBlock Text="Secondary info" Foreground="{StaticResource FluentDarkSecondaryTextBrush}"/>
        <TextBlock Text="Muted text" Foreground="{StaticResource FluentDarkMutedTextBrush}"/>
    -->
</ResourceDictionary>
