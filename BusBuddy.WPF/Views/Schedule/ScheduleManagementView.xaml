<UserControl x:Class="BusBuddy.WPF.Views.Schedule.ScheduleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sf="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#007ACC" Padding="16,12" Margin="-20,-20,-20,10" CornerRadius="0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🚌 Schedule Management" 
                          FontSize="20" 
                          FontWeight="SemiBold" 
                          Foreground="White"/>
                <TextBlock Text="{Binding Schedules.Count, StringFormat=' ({0} schedules)'}" 
                          FontSize="14" 
                          Foreground="White" 
                          Margin="10,0,0,0" 
                          VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Data Grid -->
        <sf:SfDataGrid Grid.Row="1"
                       ItemsSource="{Binding Schedules}"
                       SelectedItem="{Binding SelectedSchedule, Mode=TwoWay}"
                       AutoGenerateColumns="False"
                       AllowSorting="True"
                       AllowFiltering="True"
                       AllowGrouping="True"
                       ShowGroupDropArea="True"
                       GridLinesVisibility="Both"
                       HeaderLinesVisibility="Both"
                       Margin="0,10">
            <sf:SfDataGrid.Columns>
                <sf:GridTextColumn MappingName="ScheduleId" 
                                  HeaderText="ID" 
                                  Width="80"
                                  IsReadOnly="True"/>
                <sf:GridTextColumn MappingName="Destination" 
                                  HeaderText="Destination"
                                  Width="200"/>
                <sf:GridComboBoxColumn MappingName="BusId" 
                                      HeaderText="Bus"
                                      Width="120"
                                      ItemsSource="{Binding DataContext.Buses, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      DisplayMemberPath="BusNumber"
                                      SelectedValuePath="BusId" />
                <sf:GridComboBoxColumn MappingName="DriverId" 
                                      HeaderText="Driver"
                                      Width="150"
                                      ItemsSource="{Binding DataContext.Drivers, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      DisplayMemberPath="FullName"
                                      SelectedValuePath="DriverId" />
                <sf:GridDateTimeColumn MappingName="ScheduleDate" 
                                      HeaderText="Schedule Date"
                                      Width="130"
                                      Pattern="FullDate"/>
                <sf:GridTimeSpanColumn MappingName="LeaveTime" 
                                      HeaderText="Departure"
                                      Width="100"/>
                <sf:GridTimeSpanColumn MappingName="EventTime" 
                                      HeaderText="Arrival"
                                      Width="100"/>
                <sf:GridTextColumn MappingName="Status" 
                                  HeaderText="Status"
                                  Width="100"/>
                <sf:GridTextColumn MappingName="Description" 
                                  HeaderText="Description"
                                  Width="*"/>
            </sf:SfDataGrid.Columns>
            
            <!-- Simplified styling without problematic properties -->
        </sf:SfDataGrid>

        <!-- Command Buttons -->
        <StackPanel Grid.Row="2" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Right" 
                   Margin="0,15,0,0">
            <Button Content="➕ Add Schedule" 
                   Command="{Binding AddScheduleCommand}" 
                   Margin="5"
                   Padding="12,8"
                   Background="#4CAF50"
                   Foreground="White"
                   BorderThickness="0"
                   FontWeight="SemiBold"
                   Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <Button Content="✏️ Update" 
                   Command="{Binding UpdateScheduleCommand}" 
                   Margin="5"
                   Padding="12,8"
                   Background="#FF9800"
                   Foreground="White"
                   BorderThickness="0"
                   FontWeight="SemiBold"
                   Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F57C00"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <Button Content="🗑️ Delete" 
                   Command="{Binding DeleteScheduleCommand}" 
                   Margin="5"
                   Padding="12,8"
                   Background="#F44336"
                   Foreground="White"
                   BorderThickness="0"
                   FontWeight="SemiBold"
                   Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D32F2F"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <Button Content="🔄 Refresh" 
                   Command="{Binding LoadSchedulesCommand}" 
                   Margin="5"
                   Padding="12,8"
                   Background="#2196F3"
                   Foreground="White"
                   BorderThickness="0"
                   FontWeight="SemiBold"
                   Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
