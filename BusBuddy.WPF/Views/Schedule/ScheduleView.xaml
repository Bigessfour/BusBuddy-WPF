<UserControl x:Class="BusBuddy.WPF.Views.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:vm="clr-namespace:BusBuddy.WPF.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="700">
    <UserControl.DataContext>
        <vm:ScheduleViewModel />
    </UserControl.DataContext>
    <Grid>
        <syncfusion:SfDataGrid x:Name="ScheduleGrid"
                               ItemsSource="{Binding Schedules}"
                               AutoGenerateColumns="False"
                               SelectionMode="Single"
                               SelectedItem="{Binding SelectedSchedule, Mode=TwoWay}">
            <syncfusion:SfDataGrid.Columns>
                <syncfusion:GridTextColumn MappingName="ScheduleId" HeaderText="ID" Width="60"/>
                <syncfusion:GridTextColumn MappingName="Route.RouteName" HeaderText="Route" Width="120"/>
                <syncfusion:GridTextColumn MappingName="Bus.BusNumber" HeaderText="Bus" Width="80"/>
                <syncfusion:GridTextColumn MappingName="Driver.DriverName" HeaderText="Driver" Width="120"/>
                <syncfusion:GridDateTimeColumn MappingName="DepartureTime" HeaderText="Departure" Width="120"/>
                <syncfusion:GridDateTimeColumn MappingName="ArrivalTime" HeaderText="Arrival" Width="120"/>
                <syncfusion:GridTextColumn MappingName="ScheduleDate" HeaderText="Date" Width="100"/>
            </syncfusion:SfDataGrid.Columns>
        </syncfusion:SfDataGrid>
        <!-- Add buttons for Add/Edit/Delete -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0">
            <Button Content="Add" Command="{Binding AddScheduleCommand}" Margin="0,0,5,0"/>
            <Button Content="Edit" Command="{Binding EditScheduleCommand}" Margin="0,0,5,0"/>
            <Button Content="Delete" Command="{Binding DeleteScheduleCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
