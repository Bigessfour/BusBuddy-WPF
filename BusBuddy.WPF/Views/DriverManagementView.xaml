<UserControl x:Class="BusBuddy.WPF.Views.DriverManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Driver Management" FontSize="24" FontWeight="Bold" HorizontalAlignment="Left" Margin="0,0,0,16" />

        <!-- Driver Availability Calendar (replace with WPF Calendar) -->
        <Calendar Grid.Row="1" x:Name="DriverAvailabilityCalendar"
                  SelectionMode="MultipleRange"
                  DisplayDateStart="{Binding CalendarStartDate}"
                  DisplayDateEnd="{Binding CalendarEndDate}"
                  Margin="0,0,0,16" />

        <!-- License Status Report Button -->
        <Button Grid.Row="2" Content="Generate License Status Report" Command="{Binding GenerateLicenseStatusReportCommand}" Width="250" Height="32" HorizontalAlignment="Left" Margin="0,16,0,8" />

        <!-- License Status Report Grid (replace with DataGrid) -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding LicenseStatusReport}" AutoGenerateColumns="True" Margin="0,8,0,0" />
    </Grid>
</UserControl>
