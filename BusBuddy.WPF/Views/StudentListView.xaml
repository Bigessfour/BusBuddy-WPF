<UserControl x:Class="BusBuddy.WPF.Views.StudentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BusBuddy.WPF.Views"
             xmlns:viewModels="clr-namespace:BusBuddy.WPF.ViewModels"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             syncfusionskin:SfSkinManager.VisualStyle="FluentDark">
    <Grid>
        <syncfusion:SfDataGrid x:Name="StudentsGrid"
                               ItemsSource="{Binding Students}"
                               AutoGenerateColumns="False"
                               AllowResizingColumns="True"
                               AllowSorting="True"
                               AllowFiltering="True"
                               ColumnSizer="Star"
                               SelectionMode="Single"
                               NavigationMode="Row"
                               Style="{StaticResource BusBuddySfDataGridStyle}"
                               syncfusionskin:SfSkinManager.VisualStyle="FluentDark">
            <syncfusion:SfDataGrid.Columns>
                <syncfusion:GridTextColumn MappingName="StudentName" HeaderText="Name" Width="150" />
                <syncfusion:GridTextColumn MappingName="StudentNumber" HeaderText="Student Number" Width="120" />
                <syncfusion:GridTextColumn MappingName="Grade" HeaderText="Grade" Width="80" />
                <syncfusion:GridTextColumn MappingName="School" HeaderText="School" Width="150" />
                <syncfusion:GridTextColumn MappingName="Route" HeaderText="Bus Route" Width="120" />
            </syncfusion:SfDataGrid.Columns>

            <!-- Context Menu for the DataGrid -->
            <syncfusion:SfDataGrid.ContextMenu>
                <ContextMenu Style="{StaticResource BusBuddyContextMenuStyle}">
                    <MenuItem Header="View Student Details"
                              Command="{Binding ViewDetailsCommand}"
                              Style="{StaticResource BusBuddyContextMenuItemStyle}"/>
                    <MenuItem Header="Edit Student"
                              Command="{Binding EditCommand}"
                              Style="{StaticResource BusBuddyContextMenuItemStyle}"/>
                    <Separator Style="{StaticResource BusBuddyContextMenuSeparatorStyle}"/>
                    <MenuItem Header="Print Student Info"
                              Command="{Binding PrintCommand}"
                              Style="{StaticResource BusBuddyContextMenuItemStyle}"/>
                </ContextMenu>
            </syncfusion:SfDataGrid.ContextMenu>

            <!-- Double-click event handler registration through XAML events -->
            <syncfusion:SfDataGrid.CellDoubleTapped>
                <EventSetter Event="CellDoubleTapped" Handler="SfDataGrid_CellDoubleTapped" />
            </syncfusion:SfDataGrid.CellDoubleTapped>
        </syncfusion:SfDataGrid>
    </Grid>
</UserControl>
