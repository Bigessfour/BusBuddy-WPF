<UserControl x:Class="BusBuddy.WPF.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BusBuddy.WPF.Views"
             xmlns:viewModels="clr-namespace:BusBuddy.WPF.ViewModels"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <SolidColorBrush x:Key="HeaderBackground" Color="SteelBlue" />
        <Style TargetType="TextBlock" x:Key="HeaderTextStyle">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="28" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
        <Style x:Key="NavigationButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Margin" Value="0,4,0,4" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#445" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- DataContext will be set via DI in code-behind -->

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!-- Sidebar Navigation -->
        <Border x:Name="SidebarBorder" Grid.Column="0" Background="#2F4F4F" Width="220">
            <StackPanel>
                <TextBlock Text="Modules" Foreground="White" FontWeight="Bold" FontSize="18" Margin="12,16,0,12" />
                <Button Style="{StaticResource NavigationButtonStyle}" Content="Bus Management" Command="{Binding NavigateToBusManagementCommand}" />
                <Button Style="{StaticResource NavigationButtonStyle}" Content="Driver Management" Command="{Binding NavigateToDriverManagementCommand}" />
                <Button Style="{StaticResource NavigationButtonStyle}" Content="Route Management" Command="{Binding NavigateToRouteManagementCommand}" />
                <Button Style="{StaticResource NavigationButtonStyle}" Content="Schedule Management (Coming Soon)" IsEnabled="False" ToolTip="This feature is under development." />
                <Button Style="{StaticResource NavigationButtonStyle}" Content="Student Management (Coming Soon)" IsEnabled="False" ToolTip="This feature is under development." />
                <Button Style="{StaticResource NavigationButtonStyle}" Content="Maintenance Tracking (Coming Soon)" IsEnabled="False" ToolTip="This feature is under development." />
                <Button Style="{StaticResource NavigationButtonStyle}" Content="Fuel Management (Coming Soon)" IsEnabled="False" ToolTip="This feature is under development." />
                <Button Style="{StaticResource NavigationButtonStyle}" Content="Activity Logging (Coming Soon)" IsEnabled="False" ToolTip="This feature is under development." />
                <Button Style="{StaticResource NavigationButtonStyle}" Content="Ticket Management (Coming Soon)" IsEnabled="False" ToolTip="This feature is under development." />
                <Button Style="{StaticResource NavigationButtonStyle}" Content="Settings" Command="{Binding NavigateToSettingsCommand}" />
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <!-- Header Panel with Hamburger Button -->
            <DockPanel Grid.Row="0" Background="{StaticResource HeaderBackground}" Height="56">
                <Button x:Name="HamburgerButton" Content="☰" Width="40" Height="40" Margin="8,8,0,8" VerticalAlignment="Center" HorizontalAlignment="Left" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="22" Cursor="Hand" Click="HamburgerButton_Click" />
                <TextBlock Text="{Binding DashboardTitle}" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Center" Margin="56,0,0,0" />
            </DockPanel>
            <!-- Dashboard Content -->
            <syncfusion:DockingManager Grid.Row="1" x:Name="dockingManager" UseDocumentContainer="True" PersistState="True" Margin="0,16,0,0">
                <ContentControl syncfusion:DockingManager.Header="Dashboard" syncfusion:DockingManager.State="Document">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <!-- Fleet Status Summary Cards (WPF Borders) -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                            <Border Width="180" Height="100" Margin="10" Background="#F0F4F8" CornerRadius="8" BorderBrush="#B0BEC5" BorderThickness="1">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="Active Buses" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" Margin="0,8,0,0" />
                                    <TextBlock Text="{Binding TotalActiveBuses}" FontSize="32" FontWeight="Bold" Foreground="Green" HorizontalAlignment="Center" />
                                </StackPanel>
                            </Border>
                            <Border Width="180" Height="100" Margin="10" Background="#F0F4F8" CornerRadius="8" BorderBrush="#B0BEC5" BorderThickness="1">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="Inactive Buses" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" Margin="0,8,0,0" />
                                    <TextBlock Text="{Binding TotalInactiveBuses}" FontSize="32" FontWeight="Bold" Foreground="DarkRed" HorizontalAlignment="Center" />
                                </StackPanel>
                            </Border>
                            <Border Width="220" Height="100" Margin="10" Background="#F0F4F8" CornerRadius="8" BorderBrush="#B0BEC5" BorderThickness="1">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="Maintenance Due" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" Margin="0,8,0,0" />
                                    <TextBlock Text="{Binding BusesWithMaintenanceDue}" FontSize="32" FontWeight="Bold" Foreground="Orange" HorizontalAlignment="Center" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                        <!-- Main Dashboard Content -->
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <syncfusion:SfChart Grid.Column="0" Margin="20" Header="Ridership Trends">
                                <syncfusion:SfChart.PrimaryAxis>
                                    <syncfusion:CategoryAxis Header="Date" />
                                </syncfusion:SfChart.PrimaryAxis>
                                <syncfusion:SfChart.SecondaryAxis>
                                    <syncfusion:NumericalAxis Header="Passengers" />
                                </syncfusion:SfChart.SecondaryAxis>
                                <syncfusion:LineSeries ItemsSource="{Binding RidershipData}" XBindingPath="Date" YBindingPath="PassengerCount" />
                            </syncfusion:SfChart>
                            <syncfusion:SfDataGrid Grid.Column="1" Margin="20" ItemsSource="{Binding BusSchedules}" AutoGenerateColumns="True" />
                        </Grid>
                    </Grid>
                </ContentControl>
            </syncfusion:DockingManager>
        </Grid>
    </Grid>
</UserControl>
