<UserControl x:Class="BusBuddy.WPF.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BusBuddy.WPF.Views"
             xmlns:converters="clr-namespace:BusBuddy.WPF.Converters"
             xmlns:viewModels="clr-namespace:BusBuddy.WPF.ViewModels"
             xmlns:panelViewModels="clr-namespace:BusBuddy.WPF.ViewModels.Panels"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:TypeConverter x:Key="TypeConverter" />
    </UserControl.Resources>

    <UserControl.DataContext>
        <viewModels:DashboardViewModel />
    </UserControl.DataContext>

    <Grid>
        <syncfusion:DockingManager x:Name="dockingManager" 
               UseDocumentContainer="True"
               ItemsSource="{Binding Panels}">
            <!-- Removed unsupported DocumentTabItemTemplate and DockWindowItemTemplate. Use supported properties or DataTemplates as per Syncfusion documentation. -->
        </syncfusion:DockingManager>
    </Grid>
</UserControl>
