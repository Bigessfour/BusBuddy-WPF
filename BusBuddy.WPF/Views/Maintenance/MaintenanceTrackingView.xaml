<UserControl x:Class="BusBuddy.WPF.Views.Maintenance.MaintenanceTrackingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add" Command="{Binding AddCommand}" Margin="0,0,10,0"/>
            <Button Content="Edit" Command="{Binding EditCommand}" Margin="0,0,10,0"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}"/>
        </StackPanel>
        
        <!-- Placeholder message overlay -->
        <Border Background="#80FFFFFF" Margin="-10" Visibility="{Binding IsInDevelopment, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Visible}">
            <TextBlock Text="Maintenance Tracking - In Development"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="Gray"/>
        </Border>
        
        <syncfusion:SfDataGrid x:Name="MaintenanceGrid"
                               ItemsSource="{Binding MaintenanceRecords}"
                               SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                               AutoGenerateColumns="False"
                               Margin="0,40,0,0"
                               IsEnabled="{Binding IsInDevelopment, Converter={StaticResource InverseBooleanConverter}, FallbackValue=False}">
            <syncfusion:SfDataGrid.Columns>
                <!-- Bus selection by BusNumber -->
                <syncfusion:GridComboBoxColumn MappingName="VehicleId"
                                              HeaderText="Bus"
                                              ItemsSource="{Binding DataContext.AvailableBuses, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              DisplayMemberPath="BusNumber"
                                              SelectedValuePath="VehicleId" />
                <syncfusion:GridDateTimeColumn MappingName="Date" HeaderText="Date"/>
                <syncfusion:GridTextColumn MappingName="OdometerReading" HeaderText="Odometer"/>
                <syncfusion:GridTextColumn MappingName="MaintenanceCompleted" HeaderText="Maintenance Completed"/>
                <syncfusion:GridTextColumn MappingName="Vendor" HeaderText="Vendor"/>
                <syncfusion:GridTextColumn MappingName="RepairCost" HeaderText="Repair Cost"/>
                <syncfusion:GridTextColumn MappingName="Status" HeaderText="Status"/>
                <syncfusion:GridTextColumn MappingName="Priority" HeaderText="Priority"/>
                <syncfusion:GridTextColumn MappingName="Notes" HeaderText="Notes"/>
            </syncfusion:SfDataGrid.Columns>
        </syncfusion:SfDataGrid>
    </Grid>
</UserControl>
