<UserControl x:Class="BusBuddy.WPF.Views.BusManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add Bus" Command="{Binding AddBusCommand}" Margin="0,0,10,0"/>
            <Button Content="Edit Bus" Command="{Binding EditBusCommand}" Margin="0,0,10,0"/>
            <Button Content="Delete Bus" Command="{Binding DeleteBusCommand}" Margin="0,0,10,0"/>
            <Button Content="Bulk Mark Inactive" Command="{Binding BulkUpdateStatusCommand}" Margin="0,0,10,0"/>
        </StackPanel>
        <syncfusion:SfDataGrid x:Name="BusGrid"
                               ItemsSource="{Binding Buses}"
                               SelectedItem="{Binding SelectedBus, Mode=TwoWay}"
                               SelectedItems="{Binding SelectedBuses, Mode=TwoWay}"
                               SelectionMode="Extended"
                               AutoGenerateColumns="False"
                               Margin="0,40,0,0">
            <syncfusion:SfDataGrid.Columns>
                <syncfusion:GridTextColumn MappingName="Number" HeaderText="Bus Number"/>
                <syncfusion:GridTextColumn MappingName="Model" HeaderText="Model"/>
                <syncfusion:GridTextColumn MappingName="Capacity" HeaderText="Capacity"/>
                <syncfusion:GridTextColumn MappingName="Status" HeaderText="Status"/>
            </syncfusion:SfDataGrid.Columns>
        </syncfusion:SfDataGrid>
    </Grid>
</UserControl>
