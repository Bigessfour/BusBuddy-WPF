<UserControl x:Class="BusBuddy.WPF.Views.Dashboard.TestDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:BusBuddy.WPF.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance viewModels:DashboardViewModel}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#007ACC" Padding="16,12">
            <TextBlock Text="TEST Dashboard - EnhancedDashboardView has XAML errors" FontSize="24" FontWeight="SemiBold" Foreground="White"/>
        </Border>
        
        <!-- Simple Content -->
        <StackPanel Grid.Row="1" Margin="20">
            <TextBlock Text="Dashboard Test View" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding TotalBuses, StringFormat='Total Buses: {0}'}" FontSize="14" Margin="0,5"/>
            <TextBlock Text="{Binding TotalDrivers, StringFormat='Total Drivers: {0}'}" FontSize="14" Margin="0,5"/>
            <TextBlock Text="{Binding TotalActiveRoutes, StringFormat='Total Routes: {0}'}" FontSize="14" Margin="0,5"/>
            <TextBlock Text="EnhancedDashboardView has XAML parsing errors that need to be fixed" FontSize="14" Foreground="Red" Margin="0,20"/>
        </StackPanel>
    </Grid>
</UserControl>
