<UserControl x:Class="BusBuddy.WPF.Views.RouteManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Route Management" FontSize="24" FontWeight="Bold" HorizontalAlignment="Left" Margin="0,0,0,16" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="Suggest Optimizations" Command="{Binding OptimizeRoutesCommand}" Width="180" Margin="0,0,16,0" />
            <Button Content="Export Assignments to CSV" Command="{Binding ExportAssignmentsCommand}" Width="200" />
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding OptimizedRoutes}" AutoGenerateColumns="True" />
    </Grid>
</UserControl>
