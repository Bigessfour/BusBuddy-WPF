<UserControl x:Class="BusBuddy.WPF.Views.RouteManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BusBuddy.WPF.ViewModels"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    <!-- DataContext will be set via DI in code-behind -->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="Zoom In" Command="{Binding ZoomInCommand}" />
            <Button Content="Zoom Out" Command="{Binding ZoomOutCommand}" Margin="5,0" />
            <Button Content="Refresh Routes" Command="{Binding LoadRoutesCommand}" />
        </StackPanel>
        <wv2:WebView2 Grid.Row="1" x:Name="MapWebView" Source="/Assets/Map/map.html" />
    </Grid>
</UserControl>
