<!--
╔══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╗
║                                            🔒 BUSBUDDY SYNCFUSION THEME COMPLIANCE REFERENCE 🔒                                                                      ║
║                                                          [UTILITY VIEW]                                                                                               ║
║  📋 REQUIRED STANDARDS: This utility view MUST follow the universal standards defined in:                                                                             ║
║     - BusBuddy.WPF\Resources\SyncfusionFluentDarkTheme.xaml (Universal Control Standards)                                                                             ║
║     - Documentation\SyncfusionThemeImplementationGuide.md (Implementation Guide)                                                                                       ║
║  📚 DEVELOPMENT REFERENCE: See Documentation\SyncfusionThemeImplementationGuide.md for copy-paste templates                                                          ║
╚══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╝
-->

<UserControl x:Class="BusBuddy.WPF.Views.LoadingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
             Style="{StaticResource BusBuddyUserControlStyle}">
    <Grid Background="{StaticResource FluentDarkBackgroundBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Bus Buddy"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="{StaticResource FluentDarkForegroundBrush}"
                       Margin="0,0,0,20"/>

            <!-- Using SfBusyIndicator for better visual appearance -->
            <syncfusion:SfBusyIndicator IsBusy="True"
                                       AnimationType="DoubleCircle"
                                       Height="70"
                                       Width="70"
                                       Style="{StaticResource BusBuddySfBusyIndicatorStyle}"
                                       syncfusionskin:SfSkinManager.VisualStyle="FluentDark"
                                       Margin="0,0,0,20"/>

            <!-- Progress bar for additional visual feedback -->
            <syncfusion:SfLinearProgressBar IsIndeterminate="True"
                                          Width="300"
                                          Height="6"
                                          Margin="0,0,0,10"
                                          Background="{StaticResource FluentDarkSurfaceBrush}"
                                          Foreground="{StaticResource FluentDarkPrimaryBrush}"
                                          syncfusionskin:SfSkinManager.VisualStyle="FluentDark"/>

            <TextBlock Text="Loading application..."
                       HorizontalAlignment="Center"
                       Foreground="{StaticResource FluentDarkSecondaryTextBrush}"/>
        </StackPanel>
    </Grid>
</UserControl>
