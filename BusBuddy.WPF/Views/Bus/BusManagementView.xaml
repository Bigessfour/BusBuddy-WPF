<UserControl x:Class="BusBuddy.WPF.Views.Bus.BusManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sf="http://schemas.syncfusion.com/wpf"
             xmlns:viewModels="clr-namespace:BusBuddy.WPF.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:BusManagementViewModel}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Bus Management" 
                   FontSize="22" 
                   FontWeight="Bold" 
                   Margin="0,0,0,15" />

        <!-- Main Grid -->
        <sf:SfDataGrid Grid.Row="1"
                       ItemsSource="{Binding Buses}"
                       SelectedItem="{Binding SelectedBus, Mode=TwoWay}"
                       AutoGenerateColumns="False"
                       AllowResizingColumns="True"
                       AllowSorting="True"
                       AllowFiltering="True"
                       ColumnSizer="Star"
                       SelectionMode="Single"
                       NavigationMode="Cell"
                       AllowEditing="True"
                       ShowRowHeader="True"
                       EnableDataVirtualization="True"
                       BorderThickness="1"
                       BorderBrush="#DDDDDD"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch">
            <sf:SfDataGrid.Columns>
                <sf:GridTextColumn MappingName="BusNumber" HeaderText="Bus Number" Width="120"/>
                <sf:GridTextColumn MappingName="Year" HeaderText="Year" Width="80"/>
                <sf:GridTextColumn MappingName="Make" HeaderText="Make" Width="120"/>
                <sf:GridTextColumn MappingName="Model" HeaderText="Model" Width="120"/>
                <sf:GridTextColumn MappingName="SeatingCapacity" HeaderText="Capacity" Width="80"/>
                <sf:GridTextColumn MappingName="Status" HeaderText="Status" Width="100"/>
                <sf:GridDateTimeColumn MappingName="DateLastInspection" HeaderText="Last Inspection" Width="140"
                                       Pattern="ShortDate"/>
                <sf:GridTextColumn MappingName="InspectionStatus" HeaderText="Inspection Status" Width="120"/>
                <sf:GridTextColumn MappingName="CurrentOdometer" HeaderText="Odometer" Width="100"/>
            </sf:SfDataGrid.Columns>
            <sf:SfDataGrid.Style>
                <Style TargetType="sf:SfDataGrid">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="GridLinesVisibility" Value="Both"/>
                </Style>
            </sf:SfDataGrid.Style>
        </sf:SfDataGrid>

        <!-- Button Panel -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Add" Command="{Binding AddBusCommand}" Margin="5" Padding="15,5" />
            <Button Content="Update" Command="{Binding UpdateBusCommand}" Margin="5" Padding="15,5" />
            <Button Content="Delete" Command="{Binding DeleteBusCommand}" Margin="5" Padding="15,5" />
        </StackPanel>
    </Grid>
</UserControl>
