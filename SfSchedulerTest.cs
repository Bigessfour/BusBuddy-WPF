using System;
using System.Windows.Forms;

// Test different possible namespaces for SfScheduler
// Commenting out to avoid errors, will uncomment one by one to test

// Option 1: Direct namespace
// using Syncfusion.SfScheduler.WinForms;

// Option 2: WinForms namespace
// using Syncfusion.WinForms.Scheduler;

// Option 3: Schedule namespace
// using Syncfusion.Schedule.WinForms;

// Option 4: UI namespace
using Syncfusion.UI.WinForms.Scheduler;

namespace Bus_Buddy
{
    public class SfSchedulerTest : Form
    {
        public SfSchedulerTest()
        {
            // Will test creating SfScheduler here once we find the correct namespace
            // var scheduler = new SfScheduler();
        }
    }
}
