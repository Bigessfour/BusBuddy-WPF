using System.Windows;

namespace BusBuddy
{
    public partial class DashboardWindow : Window
    {
        public DashboardWindow()
        {
            InitializeComponent();
            // Theme is applied via XAML resource dictionary
        }
        // No code-behind view instantiation needed; TabControl handles views
    }
}
